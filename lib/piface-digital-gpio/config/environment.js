/*jshint node:true*/
'use strict';

module.exports = function(environment) {
  var ENV = {
    modulePrefix: 'piface-digital-gpio',
    environment: environment,

      i18n: {
          defaultLocale: 'en'
      },

  };

  return ENV;
};
