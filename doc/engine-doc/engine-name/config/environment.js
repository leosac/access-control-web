'use strict';

module.exports = function (environment) {
    var ENV = {
        modulePrefix: 'blueprint',
        environment: environment,
        intl: {
            defaultLocale: 'en'
        }
    };

    return ENV;
};
