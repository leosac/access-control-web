<this.args.form.element @label={{this.label}}>
    <PowerSelect @searchEnabled={{true}} @search={{this.searchZone}} @selected={{this.selected.alias}} @placeholder={{t "zone.type_zone"}} @onChange={{fn (mut this.selected)}} as |childrenInfo|>
        {{childrenInfo.alias}}
    </PowerSelect>
</this.args.form.element>
