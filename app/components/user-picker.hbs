<this.args.form.element @label={{this.args.label}}>
  <PowerSelect @searchEnabled={{true}} @selected={{this.args.selected.username}} @onChange={{this.args.onChange}} @allowClear={{true}} @search={{this.args.searchCredential}} @placeholder={{t "username"}} as |user|>
    {{user.username}}
  </PowerSelect>
</this.args.form.element>
