<this.form.element @label={{this.label}}>
  <PowerSelect @searchEnabled={{true}} @selected={{this.selected.username}} @onChange={{this.onChange}} @allowClear={{true}} @search={{this.searchCredential}} @placeholder={{t "username"}} as |user|>
    {{user.username}}
  </PowerSelect>
</this.form.element>
