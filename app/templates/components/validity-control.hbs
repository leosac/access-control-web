<legend>{{t 'validity'}}</legend>
<BsFormElement @controlType="text" @label={{t "start_date"}} @placeholder="YYYY-MM-DD HH:mm" @disabled={{this.disabled}} @value={{mut this.startDate}} @onChange={{action (mut this.startDate)}} />

<BsFormElement @controlType="text" @label={{t "start_date.end_date"}} @placeholder="YYYY-MM-DD HH:mm" @disabled={{this.disabled}} @value={{mut this.endDate}} @onChange={{action (mut this.endDate)}} />

<BsFormElement @label={{t "enabled"}} @disabled={{this.disabled}} @controlType="checkbox" @property={{this.enabledProperty}} />