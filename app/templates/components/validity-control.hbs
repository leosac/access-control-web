<fieldset>
    <legend>{{t 'validity'}}</legend>
  {{#bs-form-element label=(t 'enabled')}}
    {{input disabled=disabled type='checkbox' name=(t 'enabled') checked=enabled}}
  {{/bs-form-element}}

  {{#bs-form-element label=(t 'start_date')}}
    {{bs-datetimepicker disabled=disabled
                        date=startDate format='YYYY-MM-DD HH:mm'
                        updateDate=(action (mut startDate))
                        minDate=minDate
                        maxDate=maxDate}}
  {{/bs-form-element}}

  {{#bs-form-element label=(t 'end_date')}}
    {{bs-datetimepicker  disabled=disabled
                         date=endDate format='YYYY-MM-DD HH:mm'
                         updateDate=(action (mut endDate))
                         minDate=minDate
                         maxDate=maxDate}}
  {{/bs-form-element}}
</fieldset>
