{{#bs-form formLayout="horizontal" model=pin action=action}}
    <fieldset>
        <legend>{{ t 'configuration'}}</legend>
      {{bs-form-element controlType="text" label=(t 'alias')
                        placeholder=(t 'alias_placeholder')
                        property="alias"}}
      {{bs-form-element controlType="text" label=(t 'pincode')
                        placeholder="1234" property="code"}}

      {{bs-form-element controlType="textarea" label=(t 'notes')
                        placeholder=(t 'notes_placeholder')
                        property="description"}}

      {{user-picker selected=pin.owner onChange=(action (mut pin.owner))}}
      {{bs-button defaultText=(t 'submit') type="primary" buttonType="submit"}}
    </fieldset>
{{/bs-form}}
