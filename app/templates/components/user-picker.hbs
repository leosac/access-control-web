<BsFormElement @label={{label}}>
  <PowerSelectTypeahead @selected={{this.selected.username}} @onChange={{onChange}} @allowClear={{true}} @search={{searchCredential}} @placeholder={{t "username"}} as |user|>
    {{user.username}}
  </PowerSelectTypeahead>
</BsFormElement>
