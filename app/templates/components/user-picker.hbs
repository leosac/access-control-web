{{#form.element label=label}}
    {{#power-select
            selected=selected
            searchField="username"
            options=allUsers
            onchange=onChange
            allowClear=true
            placeholder=(t 'username')
    as |user|}}
        {{user.username}}
    {{/power-select}}
{{/form.element}}
