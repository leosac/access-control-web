{{#if credential.isRFIDCard}}
    {{#link-to 'credentials.rfid-card' credential.id}}
        {{text}}
    {{/link-to}}
{{else}}
    {{#link-to 'credentials.pin-code' credential.id}}
        {{text}}
    {{/link-to}}
{{/if}}
