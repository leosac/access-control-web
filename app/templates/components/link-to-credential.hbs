{{#if credential.isRFIDCard}}
    {{#link-to 'credentials.rfid-card' credential.id tagName="tr"}}
        {{text}}
    {{/link-to}}
{{/if}}
{{#if credential.isPinCode}}
    {{#link-to 'credentials.pin-code' credential.id tagName="tr"}}
        {{text}}
    {{/link-to}}
{{/if}}
