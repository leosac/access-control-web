{{#bs-form formLayout="horizontal" model=this}}

  {{bs-form-element controlType="password" label="New password"
                    placeholder="New Password" property="password"}}
  {{bs-form-element controlType="password" label="Confirmation"
                    placeholder="New Password" property="password2"}}
{{/bs-form}}
