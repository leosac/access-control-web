{{#if (eq ap.controllerModule 'EVOXS')}}
    <LinkTo @route='modules.evoxs.access-point' @query={{hash id=ap.id}}>
        {{yield}}
    </LinkTo>
{{else}}
    <LinkTo @route='access-point' @query={{hash id=ap.id}}>
        {{yield}}
    </LinkTo>
{{/if}}
