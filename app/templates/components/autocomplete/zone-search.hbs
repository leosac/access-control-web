<BsFormElement @label={{this.label}}>
    <PowerSelectTypeahead @search={{action "searchZone"}} @selected={{this.selected.alias}} @placeholder={{t "zone.type_zone"}} @onChange={{action (mut this.selected)}} as |childrenInfo|>
        {{childrenInfo.alias}}
    </PowerSelectTypeahead>
</BsFormElement>
