<this.form.element @label={{this.label}}>
    <PowerSelect @searchEnabled={{true}} @search={{action "searchZone"}} @selected={{this.selected.alias}} @placeholder={{t "zone.type_zone"}} @onChange={{action (mut this.selected)}} as |childrenInfo|>
        {{childrenInfo.alias}}
    </PowerSelect>
</this.form.element>
